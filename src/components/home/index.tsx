import { FormLogin } from '../../components';

export const Home = () => {
  return (
    <section className='login'>
      <div className='container '>
        <FormLogin />
      </div>
    </section>
  );
};
